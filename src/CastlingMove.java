public class CastlingMove {
    Move kingMove;
    Move rookMove;

    public CastlingMove(Move kingMove, Move rookMove) {
        this.kingMove = kingMove;
        this.rookMove = rookMove;
    }
}

